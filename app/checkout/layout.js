export const metadata = {
  robots: { index: false, follow: false },
  title: 'Checkout',
  alternates: {
    canonical: 'https://www.genzverse.shop/checkout',
  },
};

export default function CheckoutLayout({ children }) {
  return children;
}
